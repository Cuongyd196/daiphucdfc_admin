version: "3.2"
services:
  web:
    image: "pktunganhweb:latest"
    deploy:
      replicas: 1
      restart_policy:
        condition: any
    environment:
      PORT: 80
      NODE_ENV: "production"
    ports:
      - target: 80
        published: 8081
        mode: host
